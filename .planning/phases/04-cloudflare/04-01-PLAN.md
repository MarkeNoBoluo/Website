---
phase: 04-cloudflare
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cloudflared/config.yml
  - systemd/cloudflared.service
  - CONFIGURATION.md
autonomous: true
requirements: [EXTC-01, EXTC-02]
user_setup:
  - service: cloudflare
    why: "Tunnel authentication and DNS management"
    env_vars:
      - name: CLOUDFLARE_TUNNEL_ID
        source: "Generated by `cloudflared tunnel create` command"
      - name: DOMAIN
        source: "Your registered domain (e.g., example.com)"
      - name: BLOG_SUBDOMAIN
        source: "Subdomain for blog (e.g., blog)"
      - name: STATUS_SUBDOMAIN
        source: "Subdomain for health status (e.g., status)"
    dashboard_config:
      - task: "Add domain to Cloudflare"
        location: "Cloudflare Dashboard -> Websites -> Add a Site"
      - task: "Update nameservers"
        location: "Domain registrar -> Update nameservers to Cloudflare's"

must_haves:
  truths:
    - "Cloudflare Tunnel configuration template exists with environment variable placeholders"
    - "Systemd service file defines correct dependencies and security options"
    - "Environment variables documentation updated with tunnel configuration"
  artifacts:
    - path: "cloudflared/config.yml"
      provides: "Tunnel configuration template with environment variable injection"
      contains: ["tunnel: ${CLOUDFLARE_TUNNEL_ID}", "hostname: ${BLOG_SUBDOMAIN}.${DOMAIN}", "service: http://localhost:8080"]
    - path: "systemd/cloudflared.service"
      provides: "Systemd service management for cloudflared"
      contains: ["After=blog.service", "User=pi", "Restart=always", "NoNewPrivileges=true"]
    - path: "CONFIGURATION.md"
      provides: "Updated environment variables documentation"
      contains: ["CLOUDFLARE_TUNNEL_ID", "DOMAIN", "BLOG_SUBDOMAIN", "STATUS_SUBDOMAIN"]
  key_links:
    - from: "systemd/cloudflared.service"
      to: "systemd/blog.service"
      via: "After=blog.service"
      pattern: "After=blog.service"
    - from: "cloudflared/config.yml"
      to: "environment variables"
      via: "${VARIABLE} syntax"
      pattern: "\\$\\{[A-Z_]+\\}"
---

<objective>
建立 Cloudflare Tunnel 基础配置，包括配置文件模板、systemd 服务文件和文档更新。

Purpose: 为 Cloudflare Tunnel 内网穿透创建可版本控制的基础配置，确保隧道配置可通过环境变量管理，服务可通过 systemd 自动管理。
Output: 配置文件模板、systemd 服务文件、更新的环境变量文档
</objective>

<execution_context>
@C:/Users/wddkx/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wddkx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cloudflare/04-CONTEXT.md
@.planning/phases/04-cloudflare/04-RESEARCH.md

<interfaces>
<!-- Key patterns from existing infrastructure files -->

From systemd/blog.service:
```ini
[Unit]
Description=Blog Flask Application
After=network.target
Requires=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/blog
EnvironmentFile=/home/pi/blog/.env
ExecStart=/home/pi/blog/.venv/bin/gunicorn --workers 2 --bind unix:/tmp/blog.sock wsgi:app
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

From CONFIGURATION.md (existing structure):
```markdown
## Environment Variables

### Required
- `SECRET_KEY`: Flask session secret, at least 64 characters
- `DATABASE_URL`: SQLite database path, e.g., `sqlite:///blog.db`

### Optional
- `DEBUG`: Set to `1` for debug mode (default: not set)
```

From nginx/blog.conf (shows port 8080):
```nginx
server {
    listen 8080;
    server_name localhost;
    # ... rest of config
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Cloudflare Tunnel 配置文件模板</name>
  <files>cloudflared/config.yml</files>
  <action>
创建 Cloudflare Tunnel 配置文件模板，使用环境变量占位符管理敏感信息。

配置文件结构：
1. 隧道配置：使用 `${CLOUDFLARE_TUNNEL_ID}` 环境变量
2. 凭证文件路径：使用 `${CLOUDFLARE_TUNNEL_ID}` 动态生成路径
3. Ingress 规则：
   - 博客站点：`${BLOG_SUBDOMAIN}.${DOMAIN}` → `http://localhost:8080`
   - 健康检查：`${STATUS_SUBDOMAIN}.${DOMAIN}` → `http://localhost:8080/health`
   - 默认规则：`http_status:404`

遵循用户决策：
- 使用 YAML 配置文件（非命令行参数）
- 敏感信息通过环境变量注入
- 主站点使用子域名 `blog.example.com` 模式
- 健康检查使用独立子域名 `status.example.com`

参考 cloudflare-tunnel-implementation.md 中的配置示例，确保语法正确。
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('cloudflared/config.yml'))" 2>/dev/null && echo "YAML syntax valid" || echo "YAML syntax error"</automated>
  </verify>
  <done>配置文件存在且包含所有必需的环境变量占位符，YAML 语法正确</done>
</task>

<task type="auto">
  <name>Task 2: 创建 systemd 服务文件</name>
  <files>systemd/cloudflared.service</files>
  <action>
创建 systemd 服务文件管理 cloudflared 隧道服务。

服务配置要求：
1. 依赖关系：`After=blog.service`（确保博客服务先启动）
2. 用户权限：`User=pi`（与现有博客服务一致）
3. 重启策略：`Restart=always`（服务停止后总是重启）
4. 安全选项：启用 `NoNewPrivileges=true`, `PrivateTmp=true`, `ProtectSystem=strict`
5. 环境文件：`EnvironmentFile=/home/pi/blog/.env`（加载环境变量）
6. 执行命令：`ExecStart=/usr/local/bin/cloudflared tunnel --config /home/pi/.cloudflared/config.yml run`
7. 工作目录：`WorkingDirectory=/home/pi`（避免权限问题）

遵循用户决策：
- 服务依赖现有 `blog.service`
- 以 `pi` 用户运行
- 启用标准 systemd 安全选项
- 重启策略为 `always`

参考现有 `blog.service` 的模式，确保语法正确且与现有服务管理方式一致。
  </action>
  <verify>
    <automated>systemd-analyze verify systemd/cloudflared.service 2>/dev/null && echo "Service file valid" || echo "Service file error"</automated>
  </verify>
  <done>服务文件存在且语法正确，包含所有必需的配置选项和安全设置</done>
</task>

<task type="auto">
  <name>Task 3: 更新环境变量文档</name>
  <files>CONFIGURATION.md</files>
  <action>
更新 CONFIGURATION.md 文件，添加 Cloudflare Tunnel 相关的环境变量说明。

添加以下环境变量到文档：
1. `CLOUDFLARE_TUNNEL_ID`：隧道唯一标识符，由 `cloudflared tunnel create` 生成
2. `DOMAIN`：注册的域名（例如：example.com）
3. `BLOG_SUBDOMAIN`：博客子域名（例如：blog）
4. `STATUS_SUBDOMAIN`：健康检查子域名（例如：status）

文档结构：
- 在现有环境变量部分后添加 "Cloudflare Tunnel" 小节
- 每个变量说明：用途、示例值、生成方式
- 添加部署说明：首次部署时需要运行 `cloudflared tunnel login` 和 `cloudflared tunnel create`

确保文档清晰说明：
- 哪些变量是必需的
- 如何获取这些变量的值
- 变量如何注入到配置文件中
  </action>
  <verify>
    <automated>grep -q "CLOUDFLARE_TUNNEL_ID" CONFIGURATION.md && grep -q "DOMAIN" CONFIGURATION.md && grep -q "BLOG_SUBDOMAIN" CONFIGURATION.md && grep -q "STATUS_SUBDOMAIN" CONFIGURATION.md && echo "All tunnel env vars documented" || echo "Missing tunnel env vars"</automated>
  </verify>
  <done>CONFIGURATION.md 包含所有 Cloudflare Tunnel 相关的环境变量说明</done>
</task>

</tasks>

<verification>
1. 验证所有配置文件语法正确：
   - `cloudflared/config.yml`：YAML 语法正确，包含环境变量占位符
   - `systemd/cloudflared.service`：systemd 语法正确，包含所有必需配置
   - `CONFIGURATION.md`：包含隧道环境变量文档

2. 验证配置符合用户决策：
   - 使用 YAML 配置文件（非命令行参数）
   - 敏感信息通过环境变量管理
   - 服务依赖关系正确（After=blog.service）
   - 安全选项启用
</verification>

<success_criteria>
- [ ] `cloudflared/config.yml` 文件存在，包含环境变量占位符，YAML 语法正确
- [ ] `systemd/cloudflared.service` 文件存在，语法正确，包含所有必需配置和安全选项
- [ ] `CONFIGURATION.md` 更新，包含 Cloudflare Tunnel 环境变量说明
- [ ] 所有文件已提交到 git
</success_criteria>

<output>
After completion, create `.planning/phases/04-cloudflare/04-01-SUMMARY.md`
</output>
