---
phase: 05-blog-management
plan: '04'
type: execute
wave: '1'
depends_on: []
files_modified:
  - app/auth/templates/auth/login.html
  - app/static/css/admin.css
  - app/admin/templates/admin/base.html
  - app/admin/templates/admin/list.html
  - app/admin/templates/admin/create.html
  - app/admin/templates/admin/edit.html
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - Login page uses consistent dark theme
    - Admin pages use consistent dark theme
    - Flash messages have consistent styling
    - Forms and buttons follow dark theme patterns
  artifacts:
    - path: app/static/css/admin.css
      provides: Admin-specific dark theme styles
      contains: Form styles, button styles, modal styles
    - path: app/auth/templates/auth/login.html
      provides: Login page with consistent dark theme
      contains: Form, input fields, buttons matching admin theme
---

<objective>
Unify UI styling across login page and admin pages with consistent dark theme.

Purpose: Audit and update login page styling, create admin CSS file, ensure consistent dark theme across all pages.

Output: admin.css with dark theme styles, updated login template, consistent flash message styling.
</objective>

<context>
@app/auth/templates/auth/login.html
@app/static/css/style.css
@app/templates/base.html

# Existing dark theme CSS variables (style.css)
```css
:root {
    --bg-primary: #1a1a1a;
    --bg-secondary: #2a2a2a;
    --bg-tertiary: #333;
    --text-primary: #ffffff;
    --text-secondary: #cccccc;
    --text-muted: #aaaaaa;
    --accent-primary: #64b5f6;
    --accent-hover: #42a5f5;
    --border-color: #444;
    --radius-sm: 4px;
    --radius-md: 8px;
    --radius-lg: 12px;
}
```

# Current login page has basic styling but may not use CSS variables consistently.
# Admin pages need: form styles, button styles, modal styles, editor layout styles.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and update login page for dark theme consistency</name>
  <files>app/auth/templates/auth/login.html</files>
  <action>
Review and update `app/auth/templates/auth/login.html` to use CSS variables and follow the dark theme pattern:

The existing login page is mostly fine but needs:
1. Ensure all colors use CSS variables from style.css
2. Add consistent input styling with dark theme
3. Add consistent button styling
4. Ensure flash messages match the base template style

Update the login template to use these patterns:

```html
{% extends "base.html" %}

{% block title %}Login{% endblock %}

{% block content %}
<div class="login-container">
    <div class="login-card">
        <h1>Login</h1>

        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                <div class="flash-messages">
                    {% for category, message in messages %}
                        <div class="flash {{ category }}">{{ message }}</div>
                    {% endfor %}
                </div>
            {% endif %}
        {% endwith %}

        <form method="POST" action="{{ url_for('auth.login') }}" class="login-form">
            <div class="form-group">
                <label for="username">Username</label>
                <input type="text" id="username" name="username" 
                       value="{{ username or '' }}"
                       required autofocus {% if username %}class="has-value"{% endif %}
                       autocomplete="username">
            </div>

            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required
                       autocomplete="current-password">
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary btn-block">Login</button>
            </div>
        </form>

        <div class="login-help">
            <p>Access restricted. Contact administrator if you need an account.</p>
        </div>
    </div>
</div>
{% endblock %}
```

Key changes:
- Added `autocomplete` attributes for better UX
- Wrapped form in `login-card` for consistent card styling
- Simplified flash message handling to show all categories
- Added `btn-block` for full-width button
</action>
  <verify>
    <automated>python -c "from app import create_app; app = create_app(); from flask import url_for; print('Login route:', url_for('auth.login'))"</automated>
  </verify>
  <done>Login page uses consistent dark theme with CSS variables</done>
</task>

<task type="auto">
  <name>Task 2: Create admin.css with comprehensive dark theme styles</name>
  <files>app/static/css/admin.css</files>
  <action>
Create `app/static/css/admin.css` with all admin-specific styles:

```css
/* Admin interface dark theme styles
 * Extends style.css with admin-specific components
 */

/* ===== ADMIN LAYOUT ===== */
.admin-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem 1rem;
}

.admin-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--border-color);
    flex-wrap: wrap;
    gap: 1rem;
}

.admin-header h1 {
    font-size: 2rem;
    margin: 0;
    color: var(--text-primary);
}

.admin-nav {
    display: flex;
    gap: 0.5rem;
}

.admin-nav a {
    padding: 0.5rem 1rem;
    border-radius: var(--radius-md);
    color: var(--text-secondary);
    text-decoration: none;
    transition: all 0.2s ease;
}

.admin-nav a:hover {
    background: var(--bg-tertiary);
    color: var(--text-primary);
}

.admin-nav a.active {
    background: var(--accent-primary);
    color: #000;
}

/* ===== ADMIN FILTERS ===== */
.admin-filters {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
}

.filter-btn {
    padding: 0.5rem 1rem;
    border-radius: var(--radius-md);
    background: var(--bg-secondary);
    color: var(--text-secondary);
    text-decoration: none;
    border: 1px solid var(--border-color);
    transition: all 0.2s ease;
}

.filter-btn:hover {
    background: var(--bg-tertiary);
    color: var(--text-primary);
}

.filter-btn.active {
    background: var(--accent-primary);
    color: #000;
    border-color: var(--accent-primary);
}

/* ===== ADMIN ARTICLE CARDS ===== */
.admin-card {
    position: relative;
}

.article-card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.5rem;
}

.status-badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    border-radius: var(--radius-sm);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
}

.status-badge.draft {
    background: var(--bg-tertiary);
    color: var(--text-muted);
    border: 1px solid var(--border-color);
}

.status-badge.published {
    background: rgba(76, 175, 80, 0.2);
    color: #4caf50;
    border: 1px solid #4caf50;
}

.slug-preview {
    color: var(--text-muted);
    font-size: 0.85rem;
    font-family: monospace;
}

/* ===== ADMIN FORMS ===== */
.article-form {
    max-width: 100%;
}

.form-group {
    margin-bottom: 1.5rem;
}

.form-group label {
    display: block;
    margin-bottom: 0.5rem;
    color: var(--text-secondary);
    font-weight: 500;
}

.form-group input[type="text"],
.form-group textarea,
.form-group select {
    width: 100%;
    padding: 0.75rem 1rem;
    background: var(--bg-tertiary);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-md);
    color: var(--text-primary);
    font-size: 1rem;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
}

.form-group input[type="text"]:focus,
.form-group textarea:focus,
.form-group select:focus {
    outline: none;
    border-color: var(--accent-primary);
    box-shadow: 0 0 0 3px rgba(100, 181, 246, 0.2);
}

.form-group textarea {
    min-height: 400px;
    font-family: 'SF Mono', 'Monaco', 'Consolas', monospace;
    font-size: 0.9rem;
    line-height: 1.6;
    resize: vertical;
}

.form-group select {
    cursor: pointer;
}

.status-indicator {
    display: inline-block;
    margin-left: 1rem;
    font-size: 0.85rem;
}

.status-indicator.draft {
    color: var(--text-muted);
}

.status-indicator.published {
    color: #4caf50;
}

/* ===== EDITOR LAYOUT ===== */
.editor-container {
    max-width: 1400px;
}

.editor-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin-bottom: 1.5rem;
}

@media (max-width: 900px) {
    .editor-layout {
        grid-template-columns: 1fr;
    }
}

.editor-pane,
.preview-pane {
    display: flex;
    flex-direction: column;
}

.editor-pane label,
.preview-pane label {
    margin-bottom: 0.5rem;
    color: var(--text-secondary);
    font-weight: 500;
}

.preview-pane {
    background: var(--bg-secondary);
    border-radius: var(--radius-md);
    padding: 1rem;
    border: 1px solid var(--border-color);
}

.markdown-preview {
    flex-grow: 1;
    min-height: 400px;
    overflow-y: auto;
}

.markdown-preview p {
    margin: 0.5rem 0;
}

.markdown-preview h1,
.markdown-preview h2,
.markdown-preview h3 {
    margin-top: 1.5rem;
    margin-bottom: 0.75rem;
}

.markdown-preview code {
    background: var(--bg-tertiary);
    padding: 0.2rem 0.4rem;
    border-radius: var(--radius-sm);
    font-family: monospace;
}

.markdown-preview pre {
    background: #1e1e1e;
    padding: 1rem;
    border-radius: var(--radius-md);
    overflow-x: auto;
    margin: 1rem 0;
}

.placeholder-text {
    color: var(--text-muted);
    font-style: italic;
}

/* ===== FORM ACTIONS ===== */
.form-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    align-items: center;
}

.form-actions .btn {
    padding: 0.75rem 1.5rem;
}

/* ===== ADMIN BUTTONS ===== */
.btn {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    border-radius: var(--radius-md);
    text-decoration: none;
    font-weight: 500;
    cursor: pointer;
    border: none;
    transition: all 0.2s ease;
}

.btn-primary {
    background: var(--accent-primary);
    color: #000;
}

.btn-primary:hover {
    background: var(--accent-hover);
}

.btn-secondary {
    background: var(--bg-tertiary);
    color: var(--text-primary);
    border: 1px solid var(--border-color);
}

.btn-secondary:hover {
    background: #555;
}

.btn-danger {
    background: rgba(244, 67, 54, 0.15);
    color: #f44336;
    border: 1px solid #f44336;
}

.btn-danger:hover {
    background: rgba(244, 67, 54, 0.25);
}

.btn-success {
    background: rgba(76, 175, 80, 0.15);
    color: #4caf50;
    border: 1px solid #4caf50;
}

.btn-success:hover {
    background: rgba(76, 175, 80, 0.25);
}

.btn-warning {
    background: rgba(255, 152, 0, 0.15);
    color: #ff9800;
    border: 1px solid #ff9800;
}

.btn-warning:hover {
    background: rgba(255, 152, 0, 0.25);
}

.btn-preview {
    background: transparent;
    color: var(--accent-primary);
    border: 1px solid var(--accent-primary);
}

.btn-preview:hover {
    background: rgba(100, 181, 246, 0.1);
}

.btn-block {
    display: block;
    width: 100%;
    text-align: center;
}

.btn[disabled] {
    opacity: 0.5;
    cursor: not-allowed;
}

/* ===== MODAL ===== */
.modal {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.7);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;
}

.modal-content {
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
    padding: 2rem;
    max-width: 500px;
    width: 90%;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}

.modal-content h2 {
    margin: 0 0 1rem 0;
    color: var(--text-primary);
}

.modal-content p {
    color: var(--text-secondary);
    margin-bottom: 0.5rem;
}

.modal-content .warning {
    color: #f44336;
    font-weight: 500;
}

.modal-actions {
    display: flex;
    gap: 1rem;
    margin-top: 1.5rem;
}

/* ===== LOGIN PAGE STYLES ===== */
.login-container {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: calc(100vh - 200px);
    padding: 2rem;
}

.login-card {
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
    padding: 2.5rem;
    width: 100%;
    max-width: 400px;
    box-shadow: 0 8px 32px var(--shadow-medium);
}

.login-card h1 {
    text-align: center;
    margin-bottom: 2rem;
    color: var(--text-primary);
}

.login-form .form-group {
    margin-bottom: 1.5rem;
}

.login-form input {
    width: 100%;
    padding: 0.75rem 1rem;
    background: var(--bg-tertiary);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-md);
    color: var(--text-primary);
    font-size: 1rem;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
}

.login-form input:focus {
    outline: none;
    border-color: var(--accent-primary);
    box-shadow: 0 0 0 3px rgba(100, 181, 246, 0.2);
}

.login-help {
    margin-top: 2rem;
    text-align: center;
    color: var(--text-muted);
    font-size: 0.9rem;
}

/* ===== FLASH MESSAGES (Consistent styling) ===== */
.flash-messages {
    max-width: 800px;
    margin: 1rem auto;
}

.flash {
    padding: 1rem;
    border-radius: var(--radius-md);
    margin-bottom: 0.5rem;
    border-left: 4px solid var(--accent-primary);
    background-color: var(--bg-secondary);
}

.flash.success {
    border-left-color: #4caf50;
}

.flash.error {
    border-left-color: #f44336;
}

.flash.warning {
    border-left-color: #ff9800;
}

.flash.info {
    border-left-color: var(--accent-primary);
}

/* ===== NO ARTICLES ===== */
.no-articles {
    text-align: center;
    padding: 3rem;
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
    color: var(--text-muted);
}

.no-articles a {
    color: var(--accent-primary);
}
```
</action>
  <verify>
    <automated>python -c "import os; print('admin.css exists:', os.path.exists('app/static/css/admin.css'))"</automated>
  </verify>
  <done>admin.css exists with comprehensive dark theme styles</done>
</task>

<task type="auto">
  <name>Task 3: Ensure admin templates include admin.css and follow patterns</name>
  <files>app/admin/templates/admin/base.html, app/admin/templates/admin/list.html</files>
  <action>
1. Verify `app/admin/templates/admin/base.html` includes admin.css:

```html
{% extends "base.html" %}

{% block title %}Admin - {% block admin_title %}{% endblock %}{% endblock %}

{% block extra_css %}
<link rel="stylesheet" href="{{ url_for('static', filename='css/admin.css') }}">
{% endblock %}

{% block content %}
<div class="admin-container">
    ...
</div>
{% endblock %}
```

2. Update article actions in list.html to use proper button classes:

The list.html should use these button classes:
- `btn btn-secondary` for Edit
- `btn btn-danger` for Delete
- `btn btn-success` for Publish
- `btn btn-warning` for Unpublish

Make sure all buttons use the consistent styling from admin.css.

3. Ensure the admin base template has proper navigation between list and create views.
</action>
  <verify>
    <automated>python -c "from app import create_app; app = create_app(); print('Admin CSS included in templates')"</automated>
  </verify>
  <done>Admin templates include admin.css and use consistent button classes</done>
</task>

</tasks>

<verification>
Check that all admin templates render correctly with dark theme by examining the template files.
</verification>

<success_criteria>
1. Login page uses consistent dark theme with CSS variables
2. Admin pages use admin.css with consistent styling
3. Flash messages have consistent styling across all pages
4. Forms and buttons follow dark theme patterns
5. Modal dialogs match the dark theme
</success_criteria>

<output>
After completion, create `.planning/phases/05-blog-management/05-04-SUMMARY.md` and run:
```
python -m pytest tests/ -v
```
Commit: `feat(05-04): unify UI with dark theme across login and admin pages`
</output>
