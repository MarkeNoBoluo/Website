---
phase: 02-flask-application-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/__init__.py
  - app/config.py
  - app/db.py
  - app/templates/base.html
  - app/templates/errors/500.html
  - init_db.py
  - wsgi.py
  - app/blog/__init__.py
  - app/todo/__init__.py
autonomous: true
requirements: [AUTH-01, AUTH-02]
user_setup: []

must_haves:
  truths:
    - "Application factory create_app() exists and can be imported"
    - "Configuration loads from .env file with validation"
    - "Database connection with WAL mode is established"
    - "Base template structure exists for other templates to extend"
    - "Blog and todo blueprint placeholders exist"
  artifacts:
    - path: "app/__init__.py"
      provides: "Application factory create_app() function"
      exports: ["create_app"]
    - path: "app/config.py"
      provides: "Configuration class loading .env variables"
      contains: "class Config"
    - path: "app/db.py"
      provides: "SQLite connection with WAL mode setup"
      exports: ["get_db", "close_db"]
    - path: "app/templates/base.html"
      provides: "Base template structure"
      min_lines: 20
    - path: "init_db.py"
      provides: "Database initialization script"
      contains: "CREATE TABLE users"
    - path: "app/blog/__init__.py"
      provides: "Blog blueprint placeholder"
      exports: ["bp"]
    - path: "app/todo/__init__.py"
      provides: "Todo blueprint placeholder"
      exports: ["bp"]
  key_links:
    - from: "wsgi.py"
      to: "app.create_app()"
      via: "import and function call"
      pattern: "from app import create_app"
    - from: "app/db.py"
      to: "app.config"
      via: "current_app.config['DATABASE_PATH']"
      pattern: "current_app\\.config\\["
    - from: "app/__init__.py"
      to: "app.db.close_db"
      via: "teardown_appcontext registration"
      pattern: "app\\.teardown_appcontext"
    - from: "app/__init__.py"
      to: "app.blog.bp"
      via: "blueprint registration"
      pattern: "app\\.register_blueprint\\(blog_bp\\)"
    - from: "app/__init__.py"
      to: "app.todo.bp"
      via: "blueprint registration"
      pattern: "app\\.register_blueprint\\(todo_bp\\)"
---

<objective>
Establish Flask application foundation with application factory pattern, configuration loading, database connection with WAL mode, and base template structure.

Purpose: Create the core application structure that authentication and other features will build upon. Refactor existing app.py into proper application factory pattern for better testing and modularity.

Output: Working Flask application factory, configuration class, database connection with WAL mode, base templates, blog/todo blueprint placeholders, and updated Gunicorn entry point.
</objective>

<execution_context>
@C:/Users/wddkx/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wddkx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts from existing codebase that executors need -->

From app.py (existing):
```python
def validate_environment():
    """Validate all required environment variables."""
    # Validates SECRET_KEY, DATABASE_URL, DEBUG, LOG_LEVEL

# Flask app configuration
app.config['SECRET_KEY'] = os.getenv('SECRET_KEY')
app.config['DEBUG'] = os.getenv('DEBUG', 'false').lower() == 'true'

# Routes
@app.route('/')
def index():
    return 'Hello, world!'

@app.route('/health')
def health():
    return jsonify({'status': 'ok', 'timestamp': ...})

@app.route('/config-test')
def config_test():
    return jsonify({'debug': app.config['DEBUG'], ...})
```

From wsgi.py (existing):
```python
from app import app
# Gunicorn uses this 'app' object
```

From .env.example (existing):
```
SECRET_KEY=your-secret-key-here-must-be-at-least-64-characters
DATABASE_URL=sqlite:///blog.db
DEBUG=false
LOG_LEVEL=INFO
```

From requirements.txt (existing):
```
Flask==3.1.3
python-dotenv==1.0.1
pytest==8.2.2
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create application factory and configuration</name>
  <files>app/__init__.py, app/config.py, test_app_factory.py</files>
  <behavior>
    - Test 1: create_app() returns Flask application instance
    - Test 2: Configuration loads SECRET_KEY, DATABASE_URL, DEBUG, LOG_LEVEL from .env
    - Test 3: Configuration validation fails with missing SECRET_KEY
    - Test 4: Configuration validation fails with invalid DATABASE_URL
    - Test 5: Application factory registers teardown for database connection
  </behavior>
  <action>
    Refactor existing app.py into application factory pattern:

    1. Create app/__init__.py with create_app() factory function:
       - Takes config_class parameter (defaults to Config)
       - Creates Flask app instance: app = Flask(__name__)
       - Loads configuration: app.config.from_object(config_class)
       - Registers database teardown: app.teardown_appcontext(close_db)
       - Returns app instance

    2. Create app/config.py with Config class:
       - Based on SQLAlchemy: SQLALCHEMY_DATABASE_URI = DATABASE_URL
       - SECRET_KEY from environment
       - DEBUG from environment (bool)
       - LOG_LEVEL from environment
       - DATABASE_PATH derived from DATABASE_URL (extract path after 'sqlite:///')
       - Include validate() method that replicates existing validate_environment() logic
       - Use @property for DATABASE_PATH to compute from DATABASE_URL

    3. Update existing routes to be registered inside create_app():
       - Move /, /health, /config-test routes into create_app() function
       - Keep same functionality

    4. Remove old app.py file after successful migration

    Important: Maintain backward compatibility for existing tests. The new structure should pass all existing test_app.py tests.
  </action>
  <verify>
    <automated>python -m pytest test_app_factory.py -xvs</automated>
  </verify>
  <done>Application factory exists, configuration loads from .env, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement database connection with WAL mode</name>
  <files>app/db.py, test_db.py</files>
  <behavior>
    - Test 1: get_db() returns SQLite connection object
    - Test 2: Connection has WAL mode enabled (PRAGMA journal_mode=WAL)
    - Test 3: Connection has synchronous=NORMAL setting
    - Test 4: close_db() closes connection and removes from g
    - Test 5: Multiple calls to get_db() return same connection within request
  </behavior>
  <action>
    Create database connection management with WAL mode:

    1. Create app/db.py with:
       - get_db() function that returns SQLite connection stored in Flask's g object
       - Connection configured with WAL mode: PRAGMA journal_mode=WAL; PRAGMA synchronous=NORMAL; PRAGMA busy_timeout=5000;
       - Uses current_app.config['DATABASE_PATH'] for database file path
       - close_db() function that closes connection and removes from g
       - Register close_db with app.teardown_appcontext in create_app()

    2. Create /db-test route in create_app() that:
       - Tests database connection by executing PRAGMA journal_mode;
       - Returns JSON with connection status and WAL mode confirmation
       - Example response: {"status": "connected", "wal_mode": "wal", "database": "blog.db", "journal_mode": "wal"}
       - Should execute: cursor = db.execute("PRAGMA journal_mode;"); journal_mode = cursor.fetchone()[0]
       - Verify journal_mode == "wal" (not "delete" or other modes)

    3. Update existing routes to use get_db() for database access if needed

    Note: Use raw sqlite3 module, not SQLAlchemy, per user decision.
  </action>
  <verify>
    <automated>python -m pytest test_db.py -xvs</automated>
  </verify>
  <done>Database connection with WAL mode works, /db-test endpoint returns connection status with WAL mode confirmation</done>
</task>

<task type="auto">
  <name>Task 3: Create base template structure, blueprint placeholders, and update Gunicorn entry</name>
  <files>app/templates/base.html, app/templates/errors/500.html, init_db.py, wsgi.py, app/blog/__init__.py, app/todo/__init__.py</files>
  <action>
    1. Create base template structure:
       - app/templates/base.html: Basic HTML skeleton with block content
       - Include dark theme CSS link (placeholder for Phase 3)
       - Include flash message display area
       - Basic navigation structure (home, login/logout links - to be populated later)

    2. Create error page:
       - app/templates/errors/500.html: Generic error page extending base.html
       - Show error message (in development) or generic message (in production)

    3. Create database initialization script:
       - init_db.py: Creates users table with id, username, password_hash columns
       - Only creates auth tables (users), not comments or todos (deferred)
       - Sets WAL mode on database: db.execute("PRAGMA journal_mode=WAL;")
       - Can be run manually: python init_db.py

    4. Create blog and todo blueprint placeholders:
       - app/blog/__init__.py: Create Blueprint instance for blog: bp = Blueprint('blog', __name__, template_folder='templates/blog')
       - app/todo/__init__.py: Create Blueprint instance for todo: bp = Blueprint('todo', __name__, template_folder='templates/todo')
       - Register both blueprints in create_app(): app.register_blueprint(blog.bp, url_prefix='/blog'); app.register_blueprint(todo.bp, url_prefix='/todo')
       - These are placeholders for future phases - no routes yet

    5. Update Gunicorn entry point:
       - wsgi.py: Change from "from app import app" to "from app import create_app; app = create_app()"
       - Ensure backward compatibility with existing deployment

    6. Update .env.example if needed for new configuration values
  </action>
  <verify>
    <automated>python -c "from app import create_app; app = create_app(); print('App factory works, blueprints:', [bp.name for bp in app.blueprints.values()])"</automated>
  </verify>
  <done>Base templates exist, init_db.py creates users table, blog/todo blueprint placeholders created, wsgi.py updated for application factory</done>
</task>

</tasks>

<verification>
1. Run all tests: `python -m pytest test_app_factory.py test_db.py -xvs`
2. Test application factory: `python -c "from app import create_app; app = create_app(); print(app.name)"`
3. Test database connection: Start app, curl http://localhost/db-test (should return connection status with WAL mode confirmation)
4. Verify wsgi.py works: `python -c "from wsgi import app; print(type(app))"`
5. Verify blueprints registered: `python -c "from app import create_app; app = create_app(); print('Blueprints:', [bp.name for bp in app.blueprints.values()])"`
</verification>

<success_criteria>
1. Application factory create_app() exists and returns Flask app
2. Configuration loads from .env with validation
3. Database connection with WAL mode established (/db-test endpoint works and confirms WAL mode)
4. Base template structure exists
5. init_db.py creates users table
6. Blog and todo blueprint placeholders exist and are registered
7. wsgi.py updated to use application factory
8. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-flask-application-skeleton/02-01-SUMMARY.md`
</output>
