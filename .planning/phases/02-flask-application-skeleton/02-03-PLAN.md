---
phase: 02-flask-application-skeleton
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - app/auth/routes.py
  - app/auth/templates/auth/login.html
  - app/auth/templates/auth/logout.html
  - app/auth/utils.py (add login_required decorator)
  - app/templates/base.html
autonomous: true
requirements: [AUTH-01, AUTH-02]
user_setup: []

must_haves:
  truths:
    - "User can access /login and see login form with username/password fields"
    - "User can submit valid credentials and be logged in (session created)"
    - "User can submit invalid credentials and see inline error message"
    - "User can access /logout and be logged out (session destroyed)"
    - "Login required decorator exists for protecting routes"
    - "Session expires on browser close (no persistent sessions)"
    - "Base template navigation shows login/logout based on auth state"
  artifacts:
    - path: "app/auth/routes.py"
      provides: "Login and logout route handlers"
      exports: ["login", "logout"]
    - path: "app/auth/templates/auth/login.html"
      provides: "Login form template"
      contains: ["form", "username", "password", "error display"]
    - path: "app/auth/templates/auth/logout.html"
      provides: "Logout confirmation template"
      contains: ["logout confirmation"]
    - path: "app/auth/utils.py"
      provides: "login_required decorator"
      exports: ["login_required"]
    - path: "app/templates/base.html"
      provides: "Updated base template with auth-aware navigation"
      modified: true
  key_links:
    - from: "app/auth/routes.py"
      to: "app.auth.utils.verify_user"
      via: "credential verification"
      pattern: "verify_user"
    - from: "app/auth/routes.py"
      to: "session['user_id']"
      via: "session management"
      pattern: "session\\['user_id'\\]"
    - from: "app/auth/routes.py"
      to: "app.auth.utils.login_required"
      via: "route protection"
      pattern: "@login_required"
    - from: "app/auth/templates/auth/login.html"
      to: "app/templates/base.html"
      via: "template inheritance"
      pattern: "{% extends"
    - from: "app/__init__.py"
      to: "session configuration"
      via: "SESSION_PERMANENT = False"
      pattern: "SESSION_PERMANENT.*False"
---

<objective>
Implement authentication user interface: login/logout routes, form templates, and route protection decorator.

Purpose: Create the user-facing authentication interface with login form, session management, and route protection.

Output: Working login/logout functionality with forms, session management (expires on browser close), login_required decorator, and updated base template navigation.
</objective>

<execution_context>
@C:/Users/wddkx/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wddkx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-flask-application-skeleton/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 02 that this plan depends on -->

From app/auth/__init__.py (created in Plan 02):
```python
bp = Blueprint('auth', __name__, template_folder='templates/auth')
```

From app/auth/utils.py (created in Plan 02):
```python
def hash_password(password):
    """Hash password using bcrypt."""

def check_password(password_hash, password):
    """Verify password against hash."""

def create_user(username, password):
    """Create new user in database."""

def verify_user(username, password):
    """Verify username/password credentials."""
```

From app/extensions.py (created in Plan 02):
```python
bcrypt = Bcrypt()
```

From app/templates/base.html (created in Plan 01):
```html
<!DOCTYPE html>
<html>
<head>
    <title>{% block title %}Blog{% endblock %}</title>
    <!-- Base template structure -->
</head>
<body>
    {% block content %}{% endblock %}
</body>
</html>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create login_required decorator, session configuration, and protected test route</name>
  <files>app/auth/utils.py, app/auth/routes.py, app/__init__.py, test_auth_routes.py</files>
  <behavior>
    - Test 1: login_required decorator redirects to login when no session
    - Test 2: login_required allows access when session exists
    - Test 3: Protected route returns expected content when authenticated
    - Test 4: Session destruction on logout removes user_id
    - Test 5: Session expires on browser close (SESSION_PERMANENT = False)
  </behavior>
  <action>
    1. Add login_required decorator to app/auth/utils.py:
       - Uses functools.wraps
       - Checks if 'user_id' in session
       - If not, flash message and redirect to auth.login
       - Returns decorated function

    2. Configure session settings in application factory (app/__init__.py):
       - Add to create_app() after app.config.from_object(config_class):
         - app.config['SESSION_PERMANENT'] = False  # Session expires on browser close (per user decision)
         - app.config['SESSION_COOKIE_SECURE'] = False  # For local development (True in production)
         - app.config['SESSION_COOKIE_HTTPONLY'] = True  # Security: prevent JavaScript access
         - app.config['SESSION_COOKIE_SAMESITE'] = 'Lax'  # Balance security/functionality
         - app.config['PERMANENT_SESSION_LIFETIME'] = timedelta(minutes=30)  # Fallback timeout

    3. Create login and logout routes in app/auth/routes.py:
       - Import: from flask import render_template, redirect, url_for, flash, session, request
       - Import: from . import bp (the auth blueprint)

    4. Login route (/login):
       - GET: Render login form template
       - POST: Get username/password from form, verify with verify_user()
       - If valid: set session['user_id'] = username, flash success, redirect somewhere (e.g., /)
       - If invalid: flash error message, re-render form with error

    5. Logout route (/logout):
       - GET: Render logout confirmation page
       - POST: session.pop('user_id', None), flash message, redirect to login

    6. Create a protected test route (/protected-test):
       - Use @login_required decorator
       - Returns simple message "This is a protected route"

    Note: Error messages should be displayed inline below form fields per user decision.
  </action>
  <verify>
    <automated>python -m pytest test_auth_routes.py -xvs</automated>
  </verify>
  <done>Login required decorator works, session configured to expire on browser close, login/logout routes handle sessions, protected route exists</done>
</task>

<task type="auto">
  <name>Task 2: Create login/logout templates and update base template navigation</name>
  <files>app/auth/templates/auth/login.html, app/auth/templates/auth/logout.html, app/templates/base.html</files>
  <action>
    1. Create login form template (app/auth/templates/auth/login.html):
       - Extends app/templates/base.html
       - Form with method="POST", action="{{ url_for('auth.login') }}"
       - Username field: <input type="text" name="username" required>
       - Password field: <input type="password" name="password" required>
       - Submit button
       - Error display: Show flash messages for login errors
       - Inline error display below form fields (per user decision)
       - Basic styling with CSS classes for future dark theme

    2. Create logout confirmation template (app/auth/templates/auth/logout.html):
       - Extends base.html
       - Confirmation message "Are you sure you want to logout?"
       - Form with POST method to logout route
       - Submit button "Logout"
       - Cancel link back to home

    3. Update base template navigation (app/templates/base.html):
       - Add navigation section in base template
       - Show "Login" link when not authenticated: {% if not session.get('user_id') %}
       - Show "Logout" link when authenticated: {% if session.get('user_id') %}
       - Use Flask's session object in template context
       - Example structure:
         ```html
         <nav>
           <a href="{{ url_for('index') }}">Home</a>
           {% if session.get('user_id') %}
             <span>Welcome, {{ session['user_id'] }}</span>
             <a href="{{ url_for('auth.logout') }}">Logout</a>
           {% else %}
             <a href="{{ url_for('auth.login') }}">Login</a>
           {% endif %}
         </nav>
         ```

    4. Add flash message display to base template:
       - Section to display flash messages
       - Different styles for success/error/warning messages
       - Example:
         ```html
         {% with messages = get_flashed_messages(with_categories=true) %}
           {% if messages %}
             <div class="flash-messages">
               {% for category, message in messages %}
                 <div class="flash {{ category }}">{{ message }}</div>
               {% endfor %}
             </div>
           {% endif %}
         {% endwith %}
         ```

    5. Form styling considerations:
       - Use semantic HTML structure
       - Add appropriate labels for accessibility
       - Fieldset and legend for form groups
       - Placeholder text for guidance

    Note: Full dark theme styling will be added in Phase 3. Focus on functional structure now.
  </action>
  <verify>
    <automated>python -c "from app import create_app; app = create_app(); with app.test_client() as client: r = client.get('/login'); print('Login page status:', r.status_code); r = client.get('/'); print('Home page status:', r.status_code)"</automated>
  </verify>
  <done>Login and logout templates exist, forms work, base template navigation updates based on auth state, flash messages display</done>
</task>

</tasks>

<verification>
1. Test login flow: Start app, access /login, verify form displays
2. Test protected route: Access /protected-test without login → redirect to /login
3. Test session: Login with valid credentials → session created → access protected route
4. Test logout: Logout → session destroyed → protected route redirects again
5. Test session expiration: Close browser, reopen, verify session not persisted (requires manual testing)
6. Run all tests: `python -m pytest test_auth_routes.py -xvs`
</verification>

<success_criteria>
1. Login form accessible at /login with username/password fields
2. Valid credentials create session and redirect
3. Invalid credentials show inline error message
4. Logout destroys session
5. login_required decorator protects routes
6. Session expires on browser close (SESSION_PERMANENT = False)
7. Navigation shows login/logout based on auth state
8. Flash messages display correctly
9. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-flask-application-skeleton/02-03-SUMMARY.md`
</output>
