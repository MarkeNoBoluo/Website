---
phase: 02-flask-application-skeleton
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - app/auth/routes.py (add user management routes)
  - app/auth/templates/auth/users.html
  - app/auth/templates/auth/create_user.html
  - app/auth/templates/auth/change_password.html
  - app/auth/utils.py (add user management functions)
  - app/templates/base.html (add user management link)
  - test_auth_routes.py (add user management tests)
autonomous: true
requirements: [AUTH-01, AUTH-02]
user_setup: []

must_haves:
  truths:
    - "Authenticated admin can view list of all users"
    - "Authenticated admin can create new user accounts via web interface"
    - "Authenticated admin can delete user accounts (except own)"
    - "Authenticated user can change their own password"
    - "User management routes are protected with @login_required"
    - "Password strength validation enforces minimum requirements"
  artifacts:
    - path: "app/auth/routes.py"
      provides: "User management routes"
      exports: ["users", "create_user", "delete_user", "change_password"]
      modified: true
    - path: "app/auth/templates/auth/users.html"
      provides: "User listing template"
      contains: ["user list table", "create user button", "delete buttons"]
    - path: "app/auth/templates/auth/create_user.html"
      provides: "Create user form template"
      contains: ["username field", "password field", "confirm password field"]
    - path: "app/auth/templates/auth/change_password.html"
      provides: "Change password form template"
      contains: ["current password", "new password", "confirm new password"]
    - path: "app/auth/utils.py"
      provides: "User management utilities"
      exports: ["get_all_users", "delete_user", "change_user_password", "validate_password_strength"]
      modified: true
    - path: "app/templates/base.html"
      provides: "Updated navigation with user management link"
      modified: true
  key_links:
    - from: "app/auth/routes.py"
      to: "app.auth.utils.get_all_users"
      via: "user listing data"
      pattern: "get_all_users"
    - from: "app/auth/routes.py"
      to: "app.auth.utils.create_user"
      via: "new user creation"
      pattern: "create_user"
    - from: "app/auth/routes.py"
      to: "app.auth.utils.validate_password_strength"
      via: "password validation"
      pattern: "validate_password_strength"
    - from: "app/auth/templates/auth/users.html"
      to: "app/templates/base.html"
      via: "template inheritance"
      pattern: "{% extends"
    - from: "app/templates/base.html"
      to: "auth.users route"
      via: "navigation link"
      pattern: "url_for\\('auth\\.users'\\)"
---

<objective>
Add user management web interface for authenticated administrators to manage user accounts.

Purpose: Provide a web-based interface for user management (list users, create new users, delete users, change passwords) instead of relying solely on command-line tools. This enables multiple admin accounts and proper user management within the application.

Output: User management routes and templates, password strength validation, and updated navigation for authenticated users.
</objective>

<execution_context>
@C:/Users/wddkx/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wddkx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-flask-application-skeleton/02-04-SUMMARY.md

<interfaces>
<!-- Key types and contracts from previous plans -->

From app/auth/utils.py (existing from Plan 02-02):
```python
def create_user(username, password):
    """Create new user in database."""

def verify_user(username, password):
    """Verify username/password credentials."""
```

From app/auth/routes.py (existing from Plan 02-03):
```python
@bp.route('/login', methods=['GET', 'POST'])
def login():
    """Login route."""

@bp.route('/logout', methods=['GET', 'POST'])
def logout():
    """Logout route."""
```

From app/auth/utils.py (existing from Plan 02-03):
```python
def login_required(f):
    """Decorator for protected routes."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add user management utilities and password validation</name>
  <files>app/auth/utils.py, test_auth_utils.py</files>
  <behavior>
    - Test 1: get_all_users() returns list of all users from database
    - Test 2: delete_user(username) removes user from database (except current user)
    - Test 3: change_user_password(username, current_password, new_password) updates password if current password is correct
    - Test 4: validate_password_strength(password) returns True for strong passwords, False for weak
    - Test 5: validate_password_strength enforces minimum length (8 chars)
    - Test 6: validate_password_strength rejects common weak passwords
    - Test 7: delete_user prevents deleting own account (safety check)
  </behavior>
  <action>
    1. Add user management functions to app/auth/utils.py:
       - get_all_users(): Query database for all users, return list of usernames
       - delete_user(username): Delete user by username (with safety check - cannot delete own account)
       - change_user_password(username, current_password, new_password): Verify current password, then update to new hash

    2. Add password strength validation:
       - validate_password_strength(password): Check minimum length (8 characters)
       - Optionally check for common weak passwords (like "password123", "admin", etc.)
       - Return (is_valid, error_message) tuple

    3. Update existing create_user() function:
       - Add password strength validation before creating user
       - Return appropriate error if password is weak

    4. Database operations:
       - Use get_db() from app.db for database connection
       - For get_all_users: SELECT username FROM users ORDER BY username
       - For delete_user: DELETE FROM users WHERE username = ? AND username != ? (current user)
       - For change_password: UPDATE users SET password_hash = ? WHERE username = ?

    5. Error handling:
       - Handle SQLite errors appropriately
       - Provide clear error messages for user feedback

    Note: All user management functions should only be called by authenticated administrators.
  </action>
  <verify>
    <automated>python -m pytest test_auth_utils.py::test_user_management -xvs</automated>
  </verify>
  <done>User management utilities work, password validation enforces minimum requirements, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create user management routes and templates</name>
  <files>app/auth/routes.py, app/auth/templates/auth/users.html, app/auth/templates/auth/create_user.html, app/auth/templates/auth/change_password.html, test_auth_routes.py</files>
  <behavior>
    - Test 1: GET /auth/users returns user list page (requires authentication)
    - Test 2: POST /auth/users/create creates new user with valid data
    - Test 3: POST /auth/users/delete deletes user (except own)
    - Test 4: GET /auth/change-password shows password change form
    - Test 5: POST /auth/change-password updates password if current password is correct
    - Test 6: Weak passwords are rejected with error message
    - Test 7: Unauthenticated access redirects to login
  </behavior>
  <action>
    1. Add user management routes to app/auth/routes.py:
       - /auth/users (GET): Display list of all users (requires @login_required)
       - /auth/users/create (POST): Create new user (requires @login_required)
       - /auth/users/delete/<username> (POST): Delete user (requires @login_required, cannot delete self)
       - /auth/change-password (GET, POST): Change current user's password (requires @login_required)

    2. Create user management templates:
       - app/auth/templates/auth/users.html:
         * Extends base.html
         * Table showing all users (username, actions)
         * "Create New User" button linking to create form
         * Delete button for each user (except current user)
         * Confirmation for delete actions (JavaScript or separate confirmation page)

       - app/auth/templates/auth/create_user.html:
         * Extends base.html
         * Form with username, password, confirm password fields
         * Password strength requirements displayed
         * Submit button
         * Error display for validation failures

       - app/auth/templates/auth/change_password.html:
         * Extends base.html
         * Form with current password, new password, confirm new password
         * Password strength requirements
         * Submit button

    3. Form handling:
       - Use Flask's request.form for data
       - Validate all inputs (username not empty, passwords match, password strength)
       - Use flash messages for success/error feedback
       - Redirect appropriately after actions

    4. Security considerations:
       - All routes protected with @login_required
       - CSRF protection (Flask-WTF not installed, use simple session tokens or rely on same-origin)
       - Prevent self-deletion in delete route
       - Validate current password before allowing password change

    5. Update base template navigation:
       - Add "Manage Users" link to navigation when user is authenticated
       - Link to /auth/users
       - Only show to authenticated users
  </action>
  <verify>
    <automated>python -m pytest test_auth_routes.py::test_user_management_routes -xvs</automated>
  </verify>
  <done>User management routes work, templates display correctly, forms handle validation, navigation updated</done>
</task>

</tasks>

<verification>
1. Test user listing: Login, visit /auth/users, see user list
2. Test user creation: Create new user via form, verify appears in list
3. Test user deletion: Delete user (not self), verify removed from list
4. Test password change: Change own password, verify can login with new password
5. Test weak password rejection: Try to create user with "password123", see error
6. Test self-deletion prevention: Try to delete own account, see error
7. Test unauthenticated access: Without login, try to access /auth/users → redirect to login
8. Run all tests: `python -m pytest test_auth_utils.py test_auth_routes.py -xvs`
</verification>

<success_criteria>
1. Authenticated admin can view list of all users at /auth/users
2. Authenticated admin can create new users via web form
3. Authenticated admin can delete users (except own account)
4. Authenticated user can change their own password
5. Password strength validation enforces minimum 8 characters
6. All user management routes are protected with @login_required
7. Navigation includes "Manage Users" link for authenticated users
8. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-flask-application-skeleton/02-05-SUMMARY.md`
</output>
