---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .env.example
  - .gitignore
  - CONFIGURATION.md
  - app.py
  - wsgi.py
autonomous: true
requirements:
  - INFRA-01
user_setup: []

must_haves:
  truths:
    - "Python virtual environment can be created with required dependencies"
    - "Environment variables are documented and validated at startup"
    - "Git ignores sensitive files (.env, .venv/)"
    - "Minimal Flask application runs and responds to requests"
  artifacts:
    - path: "requirements.txt"
      provides: "Python dependency list"
      contains: "flask==3.1.3"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "SECRET_KEY="
    - path: ".gitignore"
      provides: "Git exclusion rules"
      contains: ".env"
    - path: "CONFIGURATION.md"
      provides: "Configuration documentation"
      min_lines: 20
    - path: "app.py"
      provides: "Flask application"
      exports: ["app"]
    - path: "wsgi.py"
      provides: "Gunicorn entry point"
      imports: ["app"]
  key_links:
    - from: "app.py"
      to: ".env.example"
      via: "config validation"
      pattern: "load_dotenv|SECRET_KEY"
    - from: "wsgi.py"
      to: "app.py"
      via: "import"
      pattern: "from app import app"
---

<objective>
Create Python foundation and minimal Flask application with environment validation.

Purpose: Establish the basic Python environment, dependency management, and a runnable Flask application that validates configuration before proceeding to production server setup.
Output: requirements.txt, .env.example, .gitignore, CONFIGURATION.md, app.py, wsgi.py
</objective>

<execution_context>
@C:/Users/wddkx/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wddkx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python environment foundation files</name>
  <files>requirements.txt, .env.example, .gitignore, CONFIGURATION.md</files>
  <action>
Create the following files:

1. **requirements.txt** - Python dependencies:
   - flask==3.1.3
   - gunicorn==21.2.0
   - python-dotenv==1.0.1
   - Add comments explaining each dependency's purpose

2. **.env.example** - Environment variable template:
   - SECRET_KEY=your-secret-key-here (64 random characters, used for Flask session signing)
   - DATABASE_URL=sqlite:///blog.db (SQLite database path)
   - DEBUG=false (production setting)
   - LOG_LEVEL=INFO (log level for production)
   - Add comments explaining each variable, validation rules, and where it's used

3. **.gitignore** - Git exclusion rules:
   - .env (environment secrets)
   - .venv/ (virtual environment)
   - __pycache__/
   - *.pyc
   - blog.db (SQLite database)
   - logs/ (application logs)
   - Use standard Python .gitignore patterns

4. **CONFIGURATION.md** - Configuration documentation:
   - Document all environment variables from .env.example
   - Include validation rules (e.g., SECRET_KEY must be 64 chars)
   - Document setup steps: copy .env.example to .env, edit values
   - Document troubleshooting: common errors and solutions
   - Keep documentation concise but complete (~30-50 lines)

Follow user decisions from CONTEXT.md: validate all required environment variables at startup, fail fast with clear error messages.
  </action>
  <verify>
    <automated>python -c "import sys; from pathlib import Path; files = ['requirements.txt', '.env.example', '.gitignore', 'CONFIGURATION.md']; print('Checking:', [f for f in files if Path(f).exists()])"</automated>
  </verify>
  <done>All four files exist with appropriate content structure</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create minimal Flask application with config validation</name>
  <files>app.py, wsgi.py</files>
  <behavior>
    - Test 1: Flask app starts without errors when all required environment variables are set
    - Test 2: Flask app fails with clear error message when SECRET_KEY is missing
    - Test 3: Root route '/' returns "Hello, world!" with 200 status
    - Test 4: Debug mode is disabled when DEBUG=false in environment
    - Test 5: Configuration is loaded from .env file via python-dotenv
  </behavior>
  <action>
Create two files:

1. **app.py** - Flask application with configuration validation:
   - Create Flask app with name 'blog'
   - Load environment variables using python-dotenv
   - Validate required variables: SECRET_KEY, DATABASE_URL, DEBUG, LOG_LEVEL
   - If any required variable is missing or invalid, raise RuntimeError with clear message
   - Set Flask configuration: SECRET_KEY from environment, DEBUG from environment (converted to bool)
   - Add root route '/' that returns "Hello, world!" plain text
   - Add health check route '/health' that returns JSON: {"status": "ok", "timestamp": current_iso}
   - Add configuration test route '/config-test' that shows loaded config (excluding SECRET_KEY)
   - Use proper error handling for missing config

2. **wsgi.py** - Gunicorn entry point:
   - Import app from app module
   - Simple file: `from app import app`
   - This is the entry point Gunicorn will use

Follow user decisions: environment variables injected via Systemd EnvironmentFile directive (not Flask .env loading) - but still support .env for development. The app should work with either.
  </action>
  <verify>
    <automated>python -c "import os; os.environ['SECRET_KEY']='test'*16; os.environ['DATABASE_URL']='sqlite:///test.db'; os.environ['DEBUG']='false'; os.environ['LOG_LEVEL']='INFO'; from app import app; print('App imported successfully')"</automated>
  </verify>
  <done>Flask application runs, validates configuration, and responds to requests</done>
</task>

</tasks>

<verification>
1. Create virtual environment: `python -m venv .venv`
2. Activate venv and install dependencies: `source .venv/bin/activate && pip install -r requirements.txt`
3. Copy .env.example to .env and set test values
4. Run Flask app: `python app.py` (should start on port 5000)
5. Test endpoints: `curl http://localhost:5000/` returns "Hello, world!"
6. Test config validation: remove SECRET_KEY from .env, app should fail with clear error
</verification>

<success_criteria>
- Python virtual environment can be created with `python -m venv .venv`
- Dependencies install without errors: `pip install -r requirements.txt`
- Flask application starts: `python app.py` runs on port 5000
- Root endpoint works: `curl http://localhost:5000/` returns "Hello, world!"
- Configuration validation works: missing SECRET_KEY causes clear error
- All files committed to git repository
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md`
</output>
