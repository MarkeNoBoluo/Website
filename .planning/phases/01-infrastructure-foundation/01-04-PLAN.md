---
phase: 01-infrastructure-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - systemd/blog.service
autonomous: false
requirements:
  - INFRA-04
user_setup: []

must_haves:
  truths:
    - "Flask application starts automatically on system boot"
    - "Service restarts automatically on failure"
    - "Service runs as 'pi' user with proper permissions"
    - "Environment variables are loaded from file"
    - "Service depends on network being available"
  artifacts:
    - path: "systemd/blog.service"
      provides: "Systemd service unit file"
      contains: "User=pi"
    - path: "systemd/blog.service"
      provides: "Restart policy"
      contains: "Restart=on-failure"
    - path: "systemd/blog.service"
      provides: "Network dependency"
      contains: "After=network.target"
    - path: "systemd/blog.service"
      provides: "Environment file"
      contains: "EnvironmentFile="
  key_links:
    - from: "systemd/blog.service"
      to: "gunicorn.conf.py"
      via: "ExecStart command"
      pattern: "gunicorn -c gunicorn.conf.py"
    - from: "systemd/blog.service"
      to: ".env file"
      via: "EnvironmentFile directive"
      pattern: "EnvironmentFile=/home/pi/blog/.env"
---

<objective>
Create systemd service for automatic startup and management of Flask application.

Purpose: Configure systemd to start Flask application on boot, restart on failure, run as pi user, load environment variables from file, and depend on network availability per user decisions.
Output: systemd/blog.service unit file
</objective>

<execution_context>
@C:/Users/wddkx/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wddkx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md
@.planning/phases/01-infrastructure-foundation/01-02-SUMMARY.md
@.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Key paths and configurations from previous plans -->
From gunicorn.conf.py:
- Gunicorn config path: `/home/pi/blog/gunicorn.conf.py`
- Working directory: `/home/pi/blog/`

From project structure:
- Application directory: `/home/pi/blog/`
- Environment file: `/home/pi/blog/.env`
- Logs directory: `/home/pi/blog/logs/`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create systemd service unit file</name>
  <files>systemd/blog.service</files>
  <action>
Create `systemd/blog.service` systemd unit file:

1. **Unit section**:
   - `Description=Blog Flask Application`
   - `After=network.target` (wait for network per user decision)
   - `Requires=network.target`

2. **Service section**:
   - `Type=simple`
   - `User=pi` (run as pi user per user decision)
   - `Group=pi`
   - `WorkingDirectory=/home/pi/blog`
   - `EnvironmentFile=/home/pi/blog/.env` (load environment variables from file)
   - `ExecStart=/home/pi/blog/.venv/bin/gunicorn -c gunicorn.conf.py`
   - `ExecReload=/bin/kill -s HUP $MAINPID`
   - `ExecStop=/bin/kill -s TERM $MAINPID`
   - `Restart=on-failure` (restart only on failure per user decision)
   - `RestartSec=5s` (wait 5 seconds before restarting)
   - `TimeoutStopSec=30` (30 seconds timeout for graceful shutdown)

3. **Install section**:
   - `WantedBy=multi-user.target` (start on boot)

4. **Logging and permissions**:
   - `StandardOutput=journal`
   - `StandardError=journal`
   - `SyslogIdentifier=blog`
   - `ProtectSystem=strict` (protect system directories)
   - `ReadWritePaths=/home/pi/blog/logs /tmp` (allow writing to logs and temp)

5. **Security hardening** (optional but recommended):
   - `NoNewPrivileges=true`
   - `PrivateTmp=true`
   - `ProtectHome=read-only`
   - `ProtectSystem=strict`

Create directory structure: `mkdir -p systemd/`
Add comments explaining each directive, especially the EnvironmentFile usage and restart policy.
Include instructions for deploying to system:
- Copy to `/etc/systemd/system/blog.service`
- Run `sudo systemctl daemon-reload`
- Run `sudo systemctl enable blog.service`
- Run `sudo systemctl start blog.service`
  </action>
  <verify>
    <automated>grep -q "User=pi" systemd/blog.service && grep -q "Restart=on-failure" systemd/blog.service && grep -q "EnvironmentFile=" systemd/blog.service && echo "Systemd service contains required directives" || echo "Missing required directives"</automated>
  </verify>
  <done>Systemd service file exists with correct user, restart policy, and environment file loading</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify systemd service functionality</name>
  <files></files>
  <action>
Human verification of systemd service installation and functionality. Claude will create the service file in Task 1, but human needs to verify it works on the Raspberry Pi system.
  </action>
  <verify>
    <automated>MISSING — Human verification required</automated>
  </verify>
  <done>Systemd service is installed, enabled, starts on boot, and runs Flask application correctly</done>
</task>

</tasks>

<verification>
1. Verify service file syntax: `systemd-analyze verify systemd/blog.service`
2. Install service: `sudo cp systemd/blog.service /etc/systemd/system/ && sudo systemctl daemon-reload`
3. Enable service: `sudo systemctl enable blog.service`
4. Start service: `sudo systemctl start blog.service`
5. Check status: `sudo systemctl status blog.service`
6. Check logs: `sudo journalctl -u blog.service -n 10`
7. Test application: `curl http://localhost/` or via socket
8. Test restart: `sudo systemctl restart blog.service`
</verification>

<success_criteria>
- Systemd service file exists with correct configuration
- Service file passes syntax verification
- Service can be installed and enabled on system
- Service starts Flask application via Gunicorn
- Service restarts on failure (on-failure policy)
- Service runs as pi user with proper permissions
- Environment variables are loaded from .env file
- Service starts after network is available
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-04-SUMMARY.md`
</output>
